version: 2

models:
  - name: fct_cascading_delays
    description: "The final fact table tracking the domino effect of flight delays."
    columns:
      - name: flight_id
        description: "The primary key of the flight."
        tests:
          - unique
          - not_null
      - name: current_departure_delay
        description: "Delay in minutes."