version: 2

models:
  - name: fct_cascading_delays
    description: "The final fact table tracking the domino effect of flight delays."
    columns:
      - name: flight_id
        tests:
          - unique
          - not_null